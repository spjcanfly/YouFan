<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spj.youfan">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".YouFanApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.user.LoginActivity" />
        <activity android:name=".activity.user.RegisterActivity" />
        <activity android:name=".activity.user.FindSecretActivity" />
        <activity android:name=".activity.start.SplashActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".activity.start.ChoiceActivity" />
        <activity android:name=".activity.start.GuideActivity" />
        <activity android:name=".activity.shop.ShopDetailActivity" />
        <activity android:name=".activity.shop.PinPaiActivity" />
        <activity android:name=".activity.shop.PinLeiDetailActivity" />
        <activity android:name=".activity.shop.ShopCarActivity"></activity>
    </application>

</manifest>
